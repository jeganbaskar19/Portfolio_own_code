// ============================================================
// BACKGROUND IMAGE CONFIGURATION
// Drop an image into /public (e.g. /public/bg-hero.jpg) and put
// its path here. Leave as null to keep the default themed
// gradient + pattern for that section — nothing breaks either way.
// ============================================================

export const backgrounds = {
  hero: null, // e.g. '/bg-hero.jpg'
  about: null,
  experience: null,
  internships: null,
  academics: null,
  projects: null,
  certifications: null,
  contact: null
};

export default backgrounds;
