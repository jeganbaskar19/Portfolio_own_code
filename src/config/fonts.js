// ============================================================
// FONT CONFIGURATION
// Change fonts for the whole site from right here.
// 1. Update the Google Fonts <link> in index.html to match.
// 2. Update the family names below.
// That's it — every component reads from these CSS variables.
// ============================================================

export const fonts = {
  display: "'Fraunces', 'Georgia', serif", // headlines, big numbers
  body: "'Manrope', 'Segoe UI', sans-serif", // paragraphs, nav, buttons
  mono: "'JetBrains Mono', 'Courier New', monospace" // eyebrows, labels, code, diff markers
};

export default fonts;
